# NearMatch Functional Health Report — 2026-08-08 07:27 IST

## OVERALL STATUS: WARNINGS

All app-functionality checks pass — the CROSS JOIN regression that hit three days running (08-05, 08-06, 08-07) is now clean, and the App Review demo account's deck and Priya conversation are both healthy. The WARNINGS rating is for two non-functional issues: (1) the task's hardcoded App Review demo account ID is now **stale** — it points at a profile that no longer exists — which risks a false CRITICAL alarm on a future automated run if not corrected; and (2) App Store Connect status/TestFlight could not be refreshed this run (browser session expired, as has happened repeatedly all week).

## App Store Review Status
Could not check this run — session expired (see Check 5 below). Last confirmed status, per 2026-08-07 evening security-report follow-up: **"Waiting for Review"**, submitted ~2026-08-03 02:20 IST. As of this report (2026-08-08 07:27 IST) that is approximately **125 hours (~5 days 5 hours)** elapsed with no reported change. Apple also posted a maintenance window for App Store Connect starting 2026-08-08 06:00 AM PDT (up to 2 hours) — timing means it may have been in progress or just ending at the moment of this check, which could independently explain today's session/redirect behavior.

## Summary
| Check | Status | Detail |
|-------|--------|--------|
| CROSS JOIN regression scan | ✅ | `SELECT ... WHERE prosrc ILIKE '%cross join%'` over all of `public` returned **zero rows** — no function in the schema contains a `cross join` anymore. |
| get_candidate_profiles / get_pending_likes join pattern | ✅ | Both: `has_left_join=true`, `has_cross_join=false`. Matches commit `131b9f7` ("Fix remaining stale CROSS JOIN...") from last night. |
| App Review demo account deck | ✅ | 9 unswiped real-photo candidates found for the *current* appreview profile (Arjun, Neha, Vikram, Ananya, Karan, Shreya, Rahul, avi, Shubhangi). No auto-fix needed. |
| App Review demo account match/conversation | ✅ | Priya match intact: `matches` id=9, `a033b7b5-a3a7-4e79-bfd3-2d5fd096f4e7` ↔ `7b5a5303-...`, created 2026-08-07 22:29:57 UTC. 3-message seeded conversation intact (ids 87, 88, 89). |
| App Store review status | ⚠️ | Could not check — browser redirected to Apple ID login (`App Store Connect` sign-in screen). Did not attempt login, per instructions. |
| TestFlight build 11 health | ⚠️ | Could not check — same session expiry, same page redirect. |
| Core RPC existence | ✅ | All 5 present: `get_candidate_profiles`, `get_pending_likes`, `get_wallet_balance_paise`, `is_premium`, `record_swipe`. |

## Findings & Actions Taken

1. **CROSS JOIN bug appears durably fixed as of last night.** Git history shows commit `131b9f7` ("Fix remaining stale CROSS JOIN in 20260724000000_proximity_matching.sql", 2026-08-08 03:42 IST) landed after yesterday's 07:26 IST CRITICAL report. Today's scan (Check 1) found **no function in the entire public schema** containing `cross join` — stronger evidence than just the two named functions being clean. Recommend continuing to watch this for at least a few more days given it has silently reverted three times before (08-05, 08-06, 08-07), each apparently via a live-database change outside the migration pipeline. No SQL was rewritten by this run (nothing needed fixing).

2. **⚠️ Operational finding — the task's hardcoded App Review demo account ID (`70da959c-6e62-4d06-8e77-d964886c0ac9`) no longer exists.** Direct lookup in `auth.users` for that ID returned zero rows. The account `appreview@nearmatch.in` now has a **different** ID: `a033b7b5-a3a7-4e79-bfd3-2d5fd096f4e7`. This lines up with last night's commit `a4206a7` ("Add seed.sql with App Store review demo fixtures", 2026-08-08 04:20 IST) — the demo account and its Priya match/conversation appear to have been recreated fresh (new match id=9, created 2026-08-07 22:29:57 UTC, vs. the old match id=8 from 2026-08-02 referenced in prior reports). Running Check 3's exact queries with the old ID returns **empty results for both the profile and the Priya match** — which would read as CRITICAL ("match missing") to a naive run. This run caught the discrepancy by cross-checking `auth.users` by email before concluding anything was actually broken, and re-ran Check 3 against the correct current ID (all healthy — see Summary table). **The scheduled task's credentials block should be updated to `a033b7b5-a3a7-4e79-bfd3-2d5fd096f4e7`** to avoid a false-positive CRITICAL alarm on the next run if this file isn't corrected first.

3. **App Store Connect browser session expired again**, same pattern as every day this week (08-03 through 08-07). Redirected straight to the Apple ID sign-in screen on the inflight-version URL. Did not attempt to log in, per instructions. Last confirmed status remains "Waiting for Review" from the 2026-08-07 evening security-report check (~125 hours since submission as of this report).

4. Deck and conversation checks (Check 3) both pass cleanly under the corrected ID — no auto-fix was needed since the raw candidate query did not return zero rows.

## Auto-fixes Applied
None. The only in-scope auto-fix (swipe-reset for an exhausted deck) was not triggered — the deck has 9 fresh candidates for the current appreview account.

## Requires Founder Action
- **Update the `nearmatch-daily-functional-check` scheduled task's hardcoded App Review demo account profile ID** from `70da959c-6e62-4d06-8e77-d964886c0ac9` to `a033b7b5-a3a7-4e79-bfd3-2d5fd096f4e7` (and note the Priya match is now id=9, not id=8) — otherwise a future run may falsely report the account/match as missing.
- **Manually re-log in to App Store Connect** in the browser session this task uses — expired again, blocking status/TestFlight checks for the second day running.
- Given "Waiting for Review" has now been the status for ~5 days with no change, and Apple's own maintenance window fell this morning, worth a manual check of App Store Connect today when convenient.
- Confirm intentionally: was the App Review demo account actually deleted and recreated last night (via `seed.sql`), or did something else change its ID? Worth a quick sanity check that this was deliberate, since an unexpected account recreation could also indicate something else reset user data.

## Next check scheduled: tomorrow same time
