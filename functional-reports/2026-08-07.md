# NearMatch Functional Health Report — 2026-08-07 07:26 IST

## OVERALL STATUS: CRITICAL

**The CROSS JOIN zero-result bug has regressed in production for a third time.** `get_candidate_profiles()` and `get_pending_likes()` are back to `cross join viewer_location v`. This morning's 06:00 IST security brief confirmed the fix as live ("not a live issue," commit `799657a`) — it has reverted again since then, in well under 24 hours. The live App Review demo account (`appreview@nearmatch.in`, no saved location) currently gets **0 candidates** from the actual `get_candidate_profiles()` RPC, confirmed by direct simulated call, despite 7 eligible unswiped real-photo profiles existing in the underlying tables.

Separately, the App Store Connect browser session has expired again — status and TestFlight could not be checked this run.

## App Store Review Status
**Could not check — browser session expired, needs manual login.** Both the inflight-version page and the TestFlight page redirected to `appstoreconnect.apple.com/login?...&authResult=FAILED`. Per task instructions, did not attempt to log in. Last confirmed status (2026-08-06 evening, per this morning's security brief): **Waiting for Review**, submitted ~2026-08-03 02:20 IST — approximately **101 hours (~4 days 5 hours)** elapsed as of this check, still no change reported by the founder since.

## Summary
| Check | Status | Detail |
|-------|--------|--------|
| CROSS JOIN regression scan | 🚨 CRITICAL | `get_candidate_profiles` and `get_pending_likes` both contain `cross join viewer_location v` against the single-row-or-none CTE `viewer_location` (filtered by `where user_id = auth.uid()`). Same bug class as the 2026-08-02 incident, now recurring for the third time. |
| get_candidate_profiles / get_pending_likes join pattern | 🚨 CRITICAL | Both functions: `has_left_join=false`, `has_cross_join=true`. Explicit regression trigger condition from the task brief. |
| App Review demo account deck | 🚨 CRITICAL | Raw table query returns 7 unswiped real-photo candidates (Arjun, Neha, Vikram, Ananya, Karan, Shreya, Rahul). But appreview account (`70da959c-...`) has no row in `profile_locations`, and a direct simulated call to the live `get_candidate_profiles()` RPC as that account returns **0 rows** right now. |
| App Review demo account match/conversation | ✅ | Priya match intact: `matches` id=8, `70da959c...` ↔ `7b5a5303...`, created 2026-08-02 13:30:52 UTC, unchanged. Seeded conversation intact: 3 messages (ids 81, 82, 83) on match_id=8. |
| App Store review status | ⚠️ | Could not check — session expired (`authResult=FAILED` on both the inflight-version and TestFlight URLs). Did not attempt login per instructions. |
| TestFlight build 11 health | ⚠️ | Could not check — same session expiry. |
| Core RPC existence | ✅ | All 5 present: `get_candidate_profiles`, `get_pending_likes`, `get_wallet_balance_paise`, `is_premium`, `record_swipe`. |

## Findings & Actions Taken

1. **CROSS JOIN bug has regressed a third time, live in production, within hours of being confirmed fixed.** `pg_proc.prosrc` for both `get_candidate_profiles()` and `get_pending_likes()` currently reads `cross join viewer_location v`, not the `left join viewer_location v on true` from migration `20260805000000_reapply_candidate_location_cross_join_fix.sql`. That fix was confirmed live as recently as this morning's 06:00 IST security brief (commit `799657a`, "not a live issue"). The local migration files on disk (`20260802000002_...` and `20260805000000_...`) are both untouched and correct — this is once again a live-database-only change made outside the migration pipeline, exactly the pattern both prior incidents (2026-08-05 and 2026-08-06) already identified and warned about. Per task instructions (Check 1), **not auto-rewriting this function** — this needs founder investigation into what is touching the live schema outside migrations, since a fourth silent revert is likely if the actual mechanism isn't found and stopped.
   - Exact problem line (both functions): `cross join viewer_location v`
   - Exact recommended fix (identical to the two prior fix migrations): `left join viewer_location v on true`

2. **Confirmed real-world reviewer impact, not just a schema-level flag.** The appreview account has never had a saved location (`profile_locations` query returns `[]`). A direct simulated RPC call impersonating that account (`get_candidate_profiles(30)` via `request.jwt.claims`) returns **0 rows** right now, even though 7 real-photo, unswiped candidate profiles exist in `public.profiles`. If Apple's reviewer opens the app today, the Discover deck will be empty — a near-certain rejection reason, and the app is still (per last known status) in "Waiting for Review."

3. **Auto-fix rule 1 does not apply and would not help here.** The literal Check 3 trigger ("returns ZERO rows from the raw table query") was not met — the raw table query returned 7 rows. The swipe-reset auto-fix is scoped to clearing exhausted swipe history, not to fixing a broken join in the RPC itself; resetting swipes would not change the RPC's 0-row result while the cross join persists. No auto-fix was applied, correctly, since the actual root cause is Finding 1, which is explicitly out of auto-fix scope (Check 1: "Do NOT auto-rewrite the function").

4. **App Store Connect browser session expired again.** Both the inflight-version page and the TestFlight page redirect to `/login` with `authResult=FAILED`. Did not attempt to log in (per instructions). App Store review status and TestFlight build 11 crash/install/feedback counts could not be refreshed this run — last known values remain those in the 2026-08-06 evening digest (Waiting for Review; submitted 2026-08-03 ~02:20 IST).

5. Priya match (id=8) and its 3-message seeded conversation (ids 81, 82, 83) remain untouched and intact.

## Auto-fixes Applied
None. The one auto-fix rule in scope (swipe-reset for an exhausted deck) was not applicable — the raw candidate-count query did not return zero, and would not have fixed the actual problem regardless. No SQL functions were rewritten, per explicit instruction.

## Requires Founder Action
- **Urgent, now a repeat pattern across three incidents (2026-08-05, 2026-08-06, 2026-08-07): identify what is reverting `get_candidate_profiles()` / `get_pending_likes()` to `cross join` outside the migration pipeline**, apparently within a single day this time. Check the Supabase SQL Editor query history / audit log, any CI or seed/reset script that might be replaying an old schema dump, and any read-replica, staging→prod sync, or scheduled job that could be pushing back an older function definition. Re-applying the fix a fourth time without finding the source will just reset the clock again — likely to recur again within hours.
- **Re-apply the `left join viewer_location v on true` fix** to both functions once the root cause above is understood or contained — currently a real user-facing outage (empty Discover deck) for any account without a saved location, including the App Review demo account.
- **Manually re-log in to App Store Connect** — the automated session has expired again. This blocks review-status and TestFlight monitoring until refreshed.
- Given the app is still (per last known status) Waiting for Review and the demo account's deck is currently broken, there's a real chance Apple's reviewer hits this exact empty-deck state today — treat the CROSS JOIN fix as time-sensitive, not just a data-hygiene item.

## Next check scheduled: tomorrow same time
