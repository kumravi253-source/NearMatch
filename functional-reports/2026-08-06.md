# NearMatch Functional Health Report — 2026-08-06 02:02 IST

## OVERALL STATUS: HEALTHY

The 2026-08-05 CROSS JOIN regression is confirmed fixed and stable. The App Store Connect session, expired for 4 consecutive days, was manually re-authenticated by the founder — review status and TestFlight data are visible again. No new issues found.

## App Store Review Status
**Waiting for Review** — unchanged from submission (~2026-08-03 02:20 IST, ~72 hours ago). Confirmed directly on the inflight-version page (`iOS App Version 1.0`, Build 11, Version 1.0.0) after the founder manually re-logged in; the session had blocked this check for the 2026-08-03 through 2026-08-05 reports.

## Summary
| Check | Status | Detail |
|-------|--------|--------|
| CROSS JOIN regression scan | ✅ | `SELECT proname FROM pg_proc ... WHERE prosrc ILIKE '%cross join%'` returned `[]` — zero functions in `public` schema contain `cross join`. Fix applied 2026-08-05 (migration `20260805000000_reapply_candidate_location_cross_join_fix.sql`) is holding. |
| get_candidate_profiles / get_pending_likes join pattern | ✅ | Both functions: `has_left_join=true`, `has_cross_join=false`. |
| App Review demo account deck | ✅ | 7 unswiped real-photo candidates in the table (same set as prior days), **and** directly verified the actual RPC now returns all 7 to the appreview account (simulated via `set local request.jwt.claims`) — confirms the fix resolves the reviewer-facing empty-deck issue found in yesterday's report, not just the raw table query. |
| App Review demo account match/conversation | ✅ | Priya match intact: `matches` id=8, unchanged. Seeded conversation intact: 3 messages on match_id=8. |
| App Store review status | ✅ | Waiting for Review, confirmed live (see above). Session working again after manual re-login. |
| TestFlight build 11 health | ✅ | Build 11: Status "Ready to Submit", 2 Installations, 6 in last 7 days, **0 Crashes**, 0 Feedback. See note below — build 11 is no longer the actively-tested build. |
| Core RPC existence | ✅ | All 5 present: `get_candidate_profiles`, `get_pending_likes`, `get_wallet_balance_paise`, `is_premium`, `record_swipe`. |

## Findings & Actions Taken

1. **CROSS JOIN regression (flagged CRITICAL in the 2026-08-05 report) is fixed and stable.** Re-scanned `pg_proc` for the `cross join` pattern — zero hits. Both `get_candidate_profiles()` and `get_pending_likes()` show `has_left_join=true, has_cross_join=false`. This was fixed and committed on 2026-08-05 (see [functional-reports/2026-08-05.md](2026-08-05.md) update section) as migration `20260805000000_reapply_candidate_location_cross_join_fix.sql`, pushed to `origin/main` at commit `799657a`.

2. **Verified fix resolves the actual reviewer-facing bug, not just the schema check.** Directly called `get_candidate_profiles()` impersonating the App Review account's JWT (`sub=70da959c-...`, which still has no saved `profile_locations` row) — it now returns 7 rows instead of the 0 it would have returned under the CROSS JOIN regression. This closes the loop on yesterday's finding that the raw table-level Check-3 query could pass while the real RPC returned zero.

3. **App Store Connect session restored via manual login.** After being expired for 4 consecutive daily runs (2026-08-03 through 2026-08-05, ~72+ hours), the founder logged in manually. Both the inflight-version page and TestFlight page now load normally without redirecting to `/login`.

4. **Note: build 11 is no longer the active TestFlight build.** Builds 12 (Aug 3, 4:05 AM) and 13 (Aug 3, 6:21 AM) were uploaded after build 11 and are newer. Build 13 is currently the one in "Testing" status (1 installation, 13 in last 7 days, 0 crashes, 0 feedback); build 11 has moved to "Ready to Submit" with no new activity. Build 11 itself remains crash-free (0 crashes) — just worth the founder knowing the task brief's "build 11" framing is now slightly stale, since testers are actually exercising build 13.

No other findings. All checks passed cleanly.

## Auto-fixes Applied
None needed. Demo account deck was not exhausted; all other checks passed without requiring the swipe-reset auto-fix.

## Requires Founder Action
- None blocking. Optional: consider updating this scheduled task's brief to track "latest TestFlight build" rather than a hardcoded "build 11", since new builds are being uploaded (12, 13) as development continues ahead of the App Store decision.

## Next check scheduled: tomorrow same time
