# NearMatch Functional Health Report — 2026-08-06 07:26 IST

## OVERALL STATUS: CRITICAL

**The CROSS JOIN zero-result bug has regressed in production for a second time — `get_candidate_profiles()` and `get_pending_likes()` are back to `cross join viewer_location v`, and the live App Review demo account (which has no saved location) now gets ZERO candidates from the actual RPC despite 7 eligible unswiped profiles existing in the underlying tables.** This is the exact bug fixed on 2026-08-02, confirmed reverted once already on 2026-08-05, re-fixed via migration `20260805000000_reapply_candidate_location_cross_join_fix.sql` (committed `799657a`), confirmed HEALTHY and stable in this morning's 02:02 IST report — and now broken again roughly 5 hours later. Both times, the live function was altered outside the migration pipeline: `schema_migrations`/local files still show the fix as applied, but the live `pg_proc` source has reverted to the pre-fix `cross join`.

Separately, the App Store Connect browser session has expired again — status and TestFlight could not be checked this run.

## App Store Review Status
**Could not check — browser session expired, needs manual login.** Navigating to the inflight-version page and to TestFlight both redirected to `appstoreconnect.apple.com/login?...&authResult=FAILED`. Per task instructions, did not attempt to log in. Last confirmed status (this morning's 2026-08-06 02:02 IST report, right after founder re-login): **Waiting for Review**, submitted ~2026-08-03 02:20 IST (~77 hours ago as of this run).

## Summary
| Check | Status | Detail |
|-------|--------|--------|
| CROSS JOIN regression scan | 🚨 CRITICAL | `get_candidate_profiles` and `get_pending_likes` both contain `cross join viewer_location v` against the single-row-or-none CTE `viewer_location` (filtered by `where user_id = auth.uid()`). Same bug class as the 2026-08-02 incident. |
| get_candidate_profiles / get_pending_likes join pattern | 🚨 CRITICAL | Both functions: `has_left_join=false`, `has_cross_join=true`. This is the explicit regression trigger condition from the task brief. |
| App Review demo account deck | 🚨 CRITICAL | Raw table query returns 7 unswiped real-photo candidates (Arjun, Neha, Vikram, Ananya, Karan, Shreya, Rahul) — looks fine at the table level. But the appreview account (`70da959c-...`) has **no row in `profile_locations`**, and a direct simulated call to the live `get_candidate_profiles()` RPC as that account returns **0 rows**. The reviewer's Discover deck is empty right now. |
| App Review demo account match/conversation | ✅ | Priya match intact: `matches` id=8, unchanged since 2026-08-02. Seeded conversation intact: 3 messages (ids 81–83) on match_id=8. |
| App Store review status | ⚠️ | Could not check — session expired (`authResult=FAILED` on both the inflight-version and TestFlight URLs). Did not attempt login per instructions. |
| TestFlight build 11 health | ⚠️ | Could not check — same session expiry. |
| Core RPC existence | ✅ | All 5 present: `get_candidate_profiles`, `get_pending_likes`, `get_wallet_balance_paise`, `is_premium`, `record_swipe`. |

## Findings & Actions Taken

1. **CROSS JOIN bug has regressed for the second time, live in production.** `pg_proc.prosrc` for both `get_candidate_profiles()` and `get_pending_likes()` currently reads `cross join viewer_location v` (confirmed via direct source dump), not the `left join viewer_location v on true` that migration `20260805000000_reapply_candidate_location_cross_join_fix.sql` applied and that this morning's report confirmed was live. The local migration file on disk is untouched and correct — this is a live-database-only change, made outside the migration pipeline, exactly matching the pattern the 2026-08-05 incident's own migration comment warned about ("must have been altered directly, e.g. via SQL Editor"). Per task instructions (Check 1), **not auto-rewriting this function** — it needs founder review of what's touching the live schema outside migrations before any further automated fix is applied, since a third silent revert would just repeat the cycle.

2. **Confirmed real-world reviewer impact, not just a schema-level flag.** The appreview account has never had a saved location (`profile_locations` query returns `[]`). A direct simulated RPC call impersonating that account (`get_candidate_profiles(30)` via `request.jwt.claims`) returns **0 rows** right now, even though 7 real-photo, unswiped candidate profiles exist in `public.profiles`. If Apple's reviewer opens the app today, the Discover deck will be empty — a near-certain rejection reason per the task brief's own framing, and the app is still in "Waiting for Review" as of the last confirmed status.

3. **Auto-fix rule 1 does not apply and would not help here.** The literal Check 3 trigger ("returns ZERO rows from the raw table query") was not met — the raw table query returned 7 rows. The swipe-reset auto-fix is scoped to clearing exhausted swipe history, not to fixing a broken join in the RPC itself; resetting swipes would not change the RPC's 0-row result while the cross join persists. No auto-fix was applied, correctly, since the actual root cause is Finding 1, which is explicitly out of auto-fix scope (Check 1: "Do NOT auto-rewrite the function").

4. **App Store Connect browser session expired again**, only ~5 hours after the founder's manual re-login this morning. Both the inflight-version page and the TestFlight page redirect to `/login` with `authResult=FAILED`. Did not attempt to log in (per instructions). App Store review status and TestFlight build 11 crash/install/feedback counts could not be refreshed this run — last known values are in the 2026-08-06 02:02 IST report (Waiting for Review; build 11 at 2 installs / 0 crashes / 0 feedback, though build 13 is the actively-tested build per that report's note).

5. Priya match (id=8) and its 3-message seeded conversation (ids 81, 82, 83) remain untouched and intact.

## Auto-fixes Applied
None. The one auto-fix rule in scope (swipe-reset for an exhausted deck) was not applicable — the raw candidate-count query did not return zero, and would not have fixed the actual problem regardless. No SQL functions were rewritten, per explicit instruction.

## Requires Founder Action
- **Urgent: identify what is reverting `get_candidate_profiles()` / `get_pending_likes()` to `cross join` outside the migration pipeline.** This is now a repeat incident (2026-08-05 and 2026-08-06, ~5 hours after the last confirmed-fixed state). Check the Supabase SQL Editor query history / audit log, any CI or seed/reset script that might be replaying an old schema dump, and any read-replica or staging→prod sync process that could be pushing back an older function definition. Until the source is found, re-applying the fix a third time only resets the clock.
- **Re-apply the LEFT JOIN fix** (same pattern as `supabase/migrations/20260805000000_reapply_candidate_location_cross_join_fix.sql`) once the root cause above is understood — otherwise it may just revert again.
- **Manually re-log in to App Store Connect** in a real browser session — the automated session has expired again (~5 hours after last login). This blocks review-status and TestFlight monitoring until refreshed.
- Given the app is still Waiting for Review and the demo account's deck is currently broken, there's a real chance Apple's reviewer hits this exact empty-deck state — treat the CROSS JOIN fix as time-sensitive, not just a data-hygiene item.

## Next check scheduled: tomorrow same time
