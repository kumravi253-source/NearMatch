# NearMatch Functional Health Report — 2026-08-05 07:26 IST

## OVERALL STATUS: CRITICAL

The exact CROSS JOIN zero-result bug fixed on 2026-08-02 has **regressed in production**. `get_candidate_profiles()` and `get_pending_likes()` are both back to `cross join viewer_location v` (no `left join ... on true`). Confirmed against yesterday's report (2026-08-04), which showed `has_left_join=true, has_cross_join=false` for both — so this broke sometime in the last ~24 hours. Worse: the App Review demo account (`appreview@nearmatch.in`) itself has **no saved location row**, meaning its Discover deck RPC will now return **zero candidates** to Apple's reviewer despite eligible profiles existing in the table.

## App Store Review Status
**Could not check — browser session expired, needs manual login.** Navigating to the inflight-version page redirected to `appstoreconnect.apple.com/login?targetUrl=...&authResult=FAILED`. This is the 3rd consecutive daily run (2026-08-03, 08-04, 08-05) blocked by an expired session. Last confirmed status remains "Waiting for Review" as of submission ~2026-08-03 02:20 IST (~53 hours ago now, unconfirmed since).

## Summary
| Check | Status | Detail |
|-------|--------|--------|
| CROSS JOIN regression scan | 🚨 | Query returned exactly 2 hits: `get_candidate_profiles` and `get_pending_likes` — both back to `cross join viewer_location v`. This is the same bug class fixed in migration `20260802000002_fix_candidate_location_cross_join.sql`. |
| get_candidate_profiles / get_pending_likes join pattern | 🚨 CRITICAL | Both show `has_cross_join=true`, `has_left_join=false`. Confirmed regression vs. yesterday's report (was `has_left_join=true`/`has_cross_join=false` on 2026-08-04). |
| App Review demo account deck | ⚠️ | Raw table query shows 7 unswiped real-photo candidates exist (same 7 as yesterday — Arjun, Neha, Vikram, Ananya, Karan, Shreya, Rahul), so the literal Check-3 auto-fix trigger (zero rows) did not fire. **But** this raw query bypasses the actual RPC. Since `appreview`'s `profile_locations` row is empty (`[]` returned), the live `get_candidate_profiles()` CROSS JOINs against 0 rows and will return **0 candidates** to the app itself — the reviewer's Discover deck is effectively empty despite this check showing "candidates exist." No auto-fix applies (deck reset wouldn't help — the bug is in the join, not the swipe history). |
| App Review demo account match/conversation | ✅ | Priya match intact: `matches` id=8, unchanged. Seeded conversation intact: 3 messages (ids 81, 82, 83) on match_id=8, unchanged. |
| App Store review status | ⚠️ | Session expired (3rd consecutive day), see above. |
| TestFlight build 11 health | ⚠️ | Same expired session — `/teams/88b30b46-fff7-4280-a49b-0bf70a44fc7b/apps/6794255698/testflight` also redirected to `/login?...&authResult=FAILED`. Could not read Installations/Crashes/Feedback for build 11. |
| Core RPC existence | ✅ | All 5 present: `get_candidate_profiles`, `get_pending_likes`, `get_wallet_balance_paise`, `is_premium`, `record_swipe`. (They exist — the problem is their join logic, not their presence.) |

## Findings & Actions Taken

1. **🚨 CRITICAL — CROSS JOIN regression in `get_candidate_profiles()` and `get_pending_likes()`.**
   Live `pg_proc.prosrc` for both functions currently contains:
   ```sql
   with viewer_location as (
     select latitude, longitude from public.profile_locations where user_id = auth.uid()
   )
   ...
   left join public.profile_locations pl on pl.user_id = p.id
   cross join viewer_location v
   ```
   This is byte-for-byte the pre-fix logic. The locally tracked migration file `supabase/migrations/20260802000002_fix_candidate_location_cross_join.sql` **does** contain the correct fix (`left join viewer_location v on true` — verified by reading the file directly), and `supabase_migrations.schema_migrations` **does** list `20260802000002_fix_candidate_location_cross_join` as applied. There is no later migration (local or in `schema_migrations`) that touches either function — the only migration after it is `20260803000000_waitlist_count_rpc`, which is unrelated.

   **Conclusion: the live database function was altered outside the tracked migration flow** — most likely a manual `create or replace function` run directly in the Supabase SQL Editor (or a dashboard restore/rollback) that reintroduced the old CROSS JOIN body without a corresponding migration file. Migration history and local files are now out of sync with what's actually running in production.

   **Recommended fix** (do not apply automatically — flagging per task rules): re-run the exact `create or replace function` statements from `supabase/migrations/20260802000002_fix_candidate_location_cross_join.sql` (lines 12–117) against production, changing both `cross join viewer_location v` occurrences to `left join viewer_location v on true`. Also worth checking the Supabase Dashboard → SQL Editor query history / logs to identify who or what ran the reverting statement, so it doesn't happen a third time.

2. **⚠️ Direct consequence for Apple review: appreview account has no saved location, so its Discover deck is currently empty in-app.**
   `SELECT * FROM public.profile_locations WHERE user_id = '70da959c-...'` returned `[]`. Combined with finding #1, `get_candidate_profiles()` CROSS JOINs the profiles table against 0 rows and returns 0 candidates — regardless of how many unswiped profiles exist. If Apple's reviewer opens Discover before this is fixed, they'll hit an empty deck, a near-certain rejection reason per the task brief. This is more urgent than a simple "deck exhausted" case, and the standard auto-fix (reset swipes) will **not** resolve it — the join needs to be fixed first (finding #1), independent of swipe history.

3. **App Store Connect session expired, 3rd consecutive day.** Same failure mode as 2026-08-03 and 2026-08-04 — redirected to Apple's login with `authResult=FAILED` for both the inflight-version page and TestFlight page. No login attempted, per task rules. App Store review status and TestFlight crash/install data have now been unobservable via this automated channel for ~53+ hours.

No other findings. Priya match/conversation and core RPC existence remain healthy and unchanged from yesterday.

## Auto-fixes Applied
None. The only defined auto-fix (reset appreview's swipes if its deck query returns zero rows) did not trigger, because the raw table-level query returned 7 rows. As explained in finding #2, that auto-fix would not have fixed the reviewer's actual empty-deck experience anyway, since the real cause is the CROSS JOIN regression, not swipe history — this needs a human to apply the SQL fix per the task's no-auto-rewrite rule.

## Requires Founder Action
1. **Highest priority:** Restore the `left join viewer_location v on true` fix to `get_candidate_profiles()` and `get_pending_likes()` in production. Exact statements are in `supabase/migrations/20260802000002_fix_candidate_location_cross_join.sql`. This is live-app-breaking for any user without a saved location — currently 10 profiles total lack one, including the App Review demo account itself.
2. Investigate how/why the fix was reverted outside the migration pipeline (check Supabase Dashboard SQL Editor history) so it can't silently recur.
3. Re-authenticate the App Store Connect browser session (manual login) — blocking App Store status and TestFlight visibility for 3 days running.

## Next check scheduled: tomorrow same time
