# NearMatch Evening Report — 2026-08-01 22:00 IST

## Today's Full Stats
- Users: +0 new (total 10)
- Waitlist: +7 new (total 78)
- Swipes: 0 | Matches: 0 | Messages: 0

## Shipped Today (commits)
Rest day — no commits landed today.

Note: the working tree has uncommitted local changes (`App.js`, `ProfileSetupScreen.js`, `.gitignore`, the proximity-matching migration, package-lock.json) plus several untracked files (`.mcp.json`, `.vscode/`, `security-reports/`, `supabase/functions/waitlist-notification/`, `supabase/migrations/20260725000000_launch_promo.sql`, `supabase/migrations/20260725000001_security_fix_rpc_grants.sql`). None of this was committed today — carried over from prior sessions.

## Security
No new full audit ran today (2026-08-01) — no `security-reports/2026-08-01.md` exists. Most recent full audit remains `2026-07-30.md` (OVERALL STATUS: 🚨 CRITICAL). No commits today touched RLS policies or the Razorpay edge function, so both critical findings remain open, now for a 3rd consecutive day:

1. **CRITICAL** — `public.profile_locations` has permissive `authenticated`-role SELECT policies with `qual: true`, exposing every user's exact lat/long to any logged-in user (RLS policies are OR'd, so the correctly-scoped owner-only policy is overridden). Physical-safety/stalking risk for a dating app. Requires founder review — not auto-fixed.
2. **CRITICAL** — `create-razorpay-order` edge function creates a live Razorpay order with only the anon key and no user JWT — no authentication enforced before hitting the paid third-party API.
3. **WARN** — 19 high-severity npm advisories only resolve via a semver-major bump (`expo`→57.0.9, `react-native`→0.86.2); not auto-applied per AGENTS.md (must check Expo v54 docs first).
4. **Note** — untracked, uncommitted migration `supabase/migrations/20260725000001_security_fix_rpc_grants.sql` still sits locally (locks down `get_wallet_balance_paise`, `get_user_id_by_email`, `is_premium` RPCs) but has still not been applied or committed.

Both criticals have now been open since at least 2026-07-30 (Razorpay possibly since 2026-07-29) with zero remediation commits across three days.

## Launch Readiness Gaps
- Apple Developer account / App Store submission: still pending (no evidence of change)
- Razorpay KYC: still pending
- EAS production build: install blocker was fixed on 7/31 (`1e72c80`), but no evidence a production build has actually been run since — status unconfirmed
- Resend domain verification for nearmatch.in email: unconfirmed, no activity today
- Google Play setup: unconfirmed, no activity today
- Both critical security findings above remain unresolved and should block a public launch (GPS exposure + unauthenticated payment order creation)

## Growth Ideas for Tomorrow
1. Post an Instagram Reel showing real app UI, captioned/targeted for the Jaipur/Indore college-town audience — tier-2 cities are largely untapped by incumbent apps and cost less per install.
2. DM the 78 waitlist signups with a referral incentive ("invite 2 friends, jump the queue") to convert a growing static waitlist into active top-of-funnel growth before app activity (currently 0 swipes/matches/messages) has anything to show them.

## Tomorrow's Priorities
1. Drop the permissive RLS policies on `profile_locations` (GPS/stalking risk) — highest-severity open item, now 3 days open, should not ship to more users until fixed
2. Add an `auth.uid()` check to `create-razorpay-order` before it creates orders
3. Apply/commit the pending `20260725000001_security_fix_rpc_grants.sql` migration
4. Run an actual EAS production build now that the install blocker is fixed (still unconfirmed since 7/31)
5. Consider running a fresh full security audit — none has run since 7/30

## Notification
Sent via ntfy — id `6aBuctCIsKra`, delivered successfully on first attempt.
