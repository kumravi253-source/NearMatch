# NearMatch Morning Brief — 2026-07-26

## Stats (last 24h)
| Metric | 24h | Total |
|---|---|---|
| Users | +0 | 10 |
| Waitlist | +9 | 22 |
| Swipes | 0 | — |
| Matches | 0 | — |
| Messages | 0 | — |

## Site & SSL
- Site: Up ✅ (HTTP 200)
- SSL cert expiry: Oct 16 2026

## Security Report Summary (from security-reports/2026-07-25-evening.md)
Yesterday's three critical `SECURITY DEFINER` RPC privilege-escalation bugs (`get_wallet_balance_paise`, `get_user_id_by_email`, `is_premium`) were fixed and verified live.

Still open:
- `create-razorpay-order` edge function still has no auth check or rate limiting — can be spammed to create real Razorpay orders indefinitely. Flagged for founder review again.
- **New/confirmed this run**: `git status` still shows the security-fix migration (`supabase/migrations/20260725000001_security_fix_rpc_grants.sql`), the `waitlist-notification` edge function, and the `launch_promo` migration as **untracked in git** — the critical fixes from yesterday remain live in the database but unversioned. This has now persisted across at least one full day; recommend committing today.
- 19 high / 8 moderate npm vulnerabilities remain, all requiring the Expo `~54.0.36`→`57.0.8` / React Native `0.81.5`→`0.86.0` major bump — deferred pending planned upgrade + regression testing.

## Git Activity (last 24h)
No new commits since yesterday evening's report — same four commits (`d02543a`, `0756133`, `7aacc77`, `e085194`) remain the most recent.

## Today's Focus
1. Commit the untracked security-fix migration + `waitlist-notification` function + `launch_promo` migration — fixes are live in prod DB but not durably recorded in version control.
2. Add auth/rate-limiting to `create-razorpay-order` edge function.
3. Personally reach out to the 9 new waitlist signups from the last 24h.

## Growth Ideas for Today
1. Post a 30-second Instagram Reel showing the real swipe/match UI, geo-tagged and hashtagged for Jaipur, Indore, Surat, and Lucknow to tap tier-2 city demand.
2. DM all 22 waitlist signups offering priority access in exchange for referring 3 friends each.

## Launch Readiness (known pending items, unchanged unless noted)
- Apple Developer account registration — pending
- Razorpay KYC verification — pending (blocks real payments)
- EAS Build for production — not yet run
- Resend domain verification for nearmatch.in email — status unchecked this run
- Google Play setup — pending

## Notification
Sent via ntfy at morning run. Message id: `xwjNeHmRjgu9`.
