# NearMatch Evening Report — 2026-08-02 22:00 IST

## Today's Full Stats
- Users: +1 new (total 11)
- Waitlist: +21 new (total 98)
- Swipes: 2 | Matches: 1 | Messages: 3

## Shipped Today (commits)
14 commits landed today (07:49–15:25 IST):
- **Security**: fixed both criticals open since 7/29–7/30 — dropped permissive `profile_locations` RLS policies that exposed exact GPS to any authenticated user (`f6f4efa`), and rate-limited + CORS-restricted `create-razorpay-order` to stop unauthenticated order spam (`ead87b5`). Also locked down SECURITY DEFINER RPC grants (`764f798`).
- **Growth**: shipped a launch promo — first 300 signups get 15 days free Premium (`671ac7d`) — and deployed waitlist email/push notifications via Resend + ntfy on signup (`7618381`, `d92bb96`, now sending from the verified nearmatch.in domain).
- **Fixes/tooling**: made bio optional in profile setup (`0b7a703`), fixed a `get_candidate_profiles` migration ordering bug (`2dd766a`), resolved an `@expo/metro-config` lockfile issue (`4a14729`), added VS Code launch config, a read-only Supabase MCP server config, and a local-preview launch config.

Working tree is clean — everything committed.

## Security
Full audit ran this morning at 08:07 IST (`2026-08-02.md`, status: WARNINGS) — findings below reflect the report as generated, but note both critical items it flagged were fixed later the same day (commits above), ahead of this evening's report:

1. ~~CRITICAL — `create-razorpay-order` accepted unauthenticated requests~~ → **fixed 15:25 IST**: added per-IP rate limiting via `check_razorpay_order_rate_limit` RPC and restricted CORS to `https://nearmatch.in`. Note: this is IP-based rate limiting, not full JWT auth — intentional per the code comment, since this is an anonymous website-checkout path with no NearMatch login. Worth a founder sanity-check that this design is acceptable long-term.
2. ~~INFO — profiles SELECT policy readable by any authenticated user~~ → unchanged, believed intentional (dating app needs to show candidate profiles); precise GPS correctly isolated in `profile_locations`, which was the actual RLS bug fixed today.
3. **Still open — WARN**: 1 high-severity npm vulnerability (`postcss`, via the `expo-constants`/`@expo/config-plugins`/`uuid` chain), only fixable via `expo@57.0.9`, a semver-major bump. Not auto-applied — AGENTS.md flags Expo as version-sensitive; must re-read the v54 docs (or v57 docs, once targeting that) before attempting.
4. 1 unconfirmed QA test account (`qadpdptest20260724@nearmatch.in`, >48h old) — no action needed.

Net: both criticals from the last several days of reports are now resolved. Remaining open item is the Expo major-version bump for the npm high-severity finding.

## Launch Readiness Gaps
- Apple Developer account / App Store submission: status unconfirmed, no evidence of change today
- Razorpay KYC: status unconfirmed, no evidence of change today
- EAS production build: still unconfirmed whether an actual production build has been run since the 7/31 install-blocker fix
- Resend domain verification for nearmatch.in email: appears to be working (waitlist notifications now sending from the verified domain per `d92bb96`) — likely resolved, but not explicitly reconfirmed today
- Google Play setup: unconfirmed, no activity today
- Both prior critical security findings are now fixed as of today; the npm high-severity vuln (Expo major bump) remains the last known open security item

## Growth Ideas for Tomorrow
1. Post an Instagram Reel showing real app UI, targeted at the Jaipur/Indore/tier-2 college-town audience — waitlist grew +21 today (largest single-day jump in recent reports) and this segment remains largely untapped by incumbent apps.
2. Email the 98 waitlist signups a "refer 2 friends, jump the queue" incentive to convert the fast-growing waitlist into pre-launch engagement, especially now that waitlist notifications (Resend) are live and can carry this message automatically.

## Tomorrow's Priorities
1. Confirm actual status of Apple Developer account registration and Razorpay KYC — these are the least-visible-in-code items and the likeliest true launch blockers at this point
2. Verify whether an EAS production build has actually been run (fixed install blocker 7/31, still unconfirmed since)
3. Scope the Expo v54→57 major-version bump — read the target-version docs per AGENTS.md before starting, since it touches `expo-constants`/`@expo/config-plugins`/`uuid`
4. Founder sanity-check on the new IP-based rate-limit design for `create-razorpay-order` (no user auth by design, since it's an anonymous checkout flow)

## Notification
Sent via ntfy — id `SF74IVSwhH0W`, delivered successfully on first attempt.
