# NearMatch Evening Digest — 2026-07-30

## Stats (today, UTC)
| Metric | New | Total |
|--------|-----|-------|
| Users | 0 | 10 |
| Waitlist | 10 | 68 |
| Swipes | 0 | — |
| Matches | 0 | — |
| Messages | 0 | — |

## Shipped Today
- `9cbda63` security: auto-fix npm audit vulnerabilities (non-breaking) — bumped `brace-expansion` 1.1.16→1.1.17 across 4 nested `node_modules` locations.

Working tree still carries the same uncommitted work as prior days:
- Modified: `.gitignore`, `App.js`, `src/screens/profile/ProfileSetupScreen.js`, `supabase/migrations/20260724000000_proximity_matching.sql`
- Untracked: `.claude/launch.json`, `.mcp.json`, `.vscode/`, `security-reports/`, `supabase/functions/waitlist-notification/`, `supabase/migrations/20260725000000_launch_promo.sql`, `supabase/migrations/20260725000001_security_fix_rpc_grants.sql`

## Security (from security-reports/2026-07-30.md, 08:07 IST)
**OVERALL STATUS: 🚨 CRITICAL**
- 🚨 **CRITICAL**: `profile_locations` has two `authenticated`-role SELECT policies with `qual: true` — exposes every user's exact lat/long to any logged-in user. Likely regression from the in-progress proximity-matching work (leftover un-renamed `replace_with_policy_name` template policy).
- 🚨 **CRITICAL**: `create-razorpay-order` edge function created a real, unauthenticated Razorpay order (`order_TJYJcYluE0pG9D`, ₹299.00) using only the anon key — no auth/JWT check before hitting the paid API.
- ⚠️ WARN: 27 npm vulnerabilities (19 high) — only fixable via a semver-major `expo`/`react-native` bump; not auto-applied.
- ⚠️ WARN: `referrals` table has the same leftover `replace_with_policy_name` blanket-read policy (redundant but should be cleaned up).
- Note: `profiles` blanket-read policy lets any authenticated user read any profile directly, bypassing block-based filtering (flagged for awareness, not changed).
- Auto-fixed: `brace-expansion` patch only. RLS/edge-function fixes require founder action, not auto-applied.

## Launch Readiness Gaps
- Apple Developer account registration — pending
- Razorpay KYC verification — pending (also now blocked behind the auth-check fix above)
- EAS Build for production — not yet run
- Resend domain verification for nearmatch.in email — pending
- Google Play setup — pending
- Untracked RPC grants migration (`20260725000001_security_fix_rpc_grants.sql`) — live on DB but would be silently dropped by a `supabase db reset`

## Tomorrow's Priorities
1. Fix the two CRITICAL security findings: drop the `qual: true` policies on `profile_locations`, and add an auth check to `create-razorpay-order` before it creates live orders.
2. Commit the in-progress work sitting in the working tree (proximity matching, launch promo, RPC grants) — the untracked migration is a silent-loss risk on any `db reset`.
3. Investigate what drove today's +10 waitlist signups (referrer/UTM/campaign) and try to repeat the channel.

## Growth Ideas for Tomorrow
- Trace the source of today's +10 waitlist signups (UTM/referrer/social) and double down on whatever channel drove it.
- Record an Instagram Reel using the real app UI (not mockups) seeded into 2-3 Jaipur/Indore college WhatsApp groups — user count has been flat at 10 while the waitlist keeps growing, so the gap is conversion, not top-of-funnel.

## Notification
Sent successfully via ntfy (message id `PAHc2546eEH5`).
