# NearMatch Evening Digest — 2026-07-26

## Stats (today)
| Metric | New Today | Total |
|--------|-----------|-------|
| Users | 0 | 10 |
| Waitlist | 0 | 22 |
| Swipes | 0 | — |
| Matches | 0 | — |
| Messages | 0 | — |

No user, waitlist, swipe, match, or message activity recorded today.

## Shipped Today
No commits made today (`git log --since 00:00` empty). However, the working tree has substantial uncommitted work in progress:
- Modified: `.gitignore`, `App.js`, `src/screens/profile/ProfileSetupScreen.js`, `supabase/migrations/20260724000000_proximity_matching.sql`
- Untracked: `.claude/launch.json`, `.mcp.json`, `.vscode/`, `security-reports/`, `supabase/functions/waitlist-notification/`, `supabase/migrations/20260725000000_launch_promo.sql`, `supabase/migrations/20260725000001_security_fix_rpc_grants.sql`

This looks like in-progress feature work (proximity matching, launch promo) plus an uncommitted security fix from yesterday's audit — none of it has landed in a commit yet.

## Security — from today's 08:11 IST report ([2026-07-26.md](2026-07-26.md))
**Overall status: CRITICAL**

- 🚨 **CRITICAL**: `supabase/functions/create-razorpay-order/index.ts` has no authentication check — a smoke test using only the public anon key successfully created a real (uncaptured) live Razorpay order (`order_THyARDkjIW7eqw`, ₹299). Anyone with the anon key (trivially extractable from the app bundle) can spam order creation against the founder's live Razorpay account with no rate limiting. Not auto-fixed — payment code needs founder review.
- ⚠️ **WARN**: 27 npm vulnerabilities (19 high, 8 moderate), all requiring a breaking Expo 54→57 / RN 0.81→0.86 upgrade. No non-breaking fix exists; not applied per AGENTS.md guidance against blind upgrades.
- ⚠️ **WARN**: `supabase/migrations/20260725000001_security_fix_rpc_grants.sql` (locks down `get_wallet_balance_paise`, `is_premium`, `get_user_id_by_email` to authenticated/service_role) is verified live on the DB but is untracked in git and missing from `schema_migrations` — at risk of being lost on a DB rebuild.
- ✅ RLS, anon access, secrets hygiene, SSL, storage bucket policy, rate limiting, and block enforcement all passed.

## Launch Readiness Gaps
Known pending items (unchanged since last check — no new information available this run):
- Apple Developer account registration — pending
- Razorpay KYC verification — pending
- EAS Build for production — not yet run
- Resend domain verification for nearmatch.in email — pending
- Google Play setup — pending
- Plus the CRITICAL Razorpay auth gap and uncommitted RPC migration above

## Tomorrow's Priorities
1. Add JWT auth check (matching `delete-account`/`verify-age` pattern) to `create-razorpay-order` edge function, and restrict CORS from `*`.
2. Commit `20260725000001_security_fix_rpc_grants.sql` to git and reconcile with `schema_migrations` (e.g. `supabase migration repair`).
3. Commit the in-progress proximity matching and launch promo work sitting in the working tree.

## Growth Ideas for Tomorrow
- **Convert the stalled waitlist**: 0 new waitlist signups today, 22 total sitting idle. Post an Instagram Reel showing the real app UI (not mockups) and send a referral code/incentive to the existing 22 waitlist users to drive word-of-mouth signups.
- **Tier-2 city seeding**: Share the waitlist link directly into 2-3 college WhatsApp groups in Jaipur or Indore — untapped market, low-cost, high-intent audience for an early dating app waitlist.

## Notification
Sent successfully via ntfy at 2026-07-26 (message id `LojVJWmy8lvn`).
