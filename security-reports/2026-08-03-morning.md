# NearMatch Morning Report — 2026-08-03 06:00 IST

## Last 24h Stats
- Users: +1 new (total 11)
- Waitlist: +22 new (total 99)
- Swipes: 3 | Matches: 1 | Messages: 3

## Site Health
- nearmatch.in: HTTP 200 (Up)
- SSL certificate expires: Oct 16 2026 (healthy, no renewal needed soon)

## Shipped in the last 24h (commits)
- `f13ca1c` Show waitlist momentum on the launch counter before Sept 1
- `4b48a62` Upgrade Expo SDK 54 to 57
- `8ef4766` Disable iPad support — app was never designed for tablet
- `596f303` Fix zero-candidate bug when viewer has no saved location
- `a7b5b25` Add security audit report history

Notably, the Expo SDK 54→57 upgrade (`4b48a62`) was flagged as a priority in yesterday evening's report specifically to clear the last known open security finding (a high-severity `postcss` npm vulnerability only fixable via `expo@57.0.9`). **`npm audit` was run this morning to verify — the postcss vuln is confirmed fixed**, and a follow-up `uuid` finding surfaced and was also resolved this morning (see Security section below). **`npm audit` now reports 0 vulnerabilities.**

## Security
`npm audit` run this morning (2026-08-03):
- Both prior CRITICAL findings (permissive `profile_locations` RLS policy exposing GPS; unauthenticated `create-razorpay-order` spam) were fixed on 2026-08-02 and remain resolved.
- **postcss high-severity vuln: CONFIRMED FIXED.** The Expo 54→57 upgrade resolved it — `expo@57.0.9` → `@expo/metro-config@57.0.7` → `postcss@8.5.25`, no longer flagged.
- **uuid moderate-severity findings (11): FIXED.** Root cause was `uuid <11.1.1` ([GHSA-w5hq-g745-h8pq](https://github.com/advisories/GHSA-w5hq-g745-h8pq)) pulled in transitively via `xcode` → `@expo/config-plugins`. No clean upstream bump existed — both `@expo/config-plugins@57.0.6` and `xcode@3.0.1` were already the latest published versions of each, neither having moved to a patched `uuid`. Since `xcode`'s only use of `uuid` is `uuid.v4()` (unaffected by this advisory, which covers v3/v5/v6), added a `package.json` `overrides` entry pinning `uuid` to `^11.1.1` to force the transitive resolution. Verified: `npm ls uuid` now shows `xcode@3.0.1 → uuid@11.1.1`, and `npx expo prebuild --no-install` completed successfully, generating a valid `ios/NearMatch.xcodeproj/project.pbxproj` with well-formed UUIDs — confirming the override doesn't break native project generation. `npm audit` now reports 0 vulnerabilities.
- **Re-confirmed clean after commit + push**: re-ran `npm audit` (0 vulnerabilities) and `npm ls postcss` (still resolves to `postcss@8.5.25` via `expo@57.0.9` → `@expo/metro-config@57.0.7`) — both fixes are holding after landing on `main` (`d09643c`).
- 1 unconfirmed QA test account (`qadpdptest20260724@nearmatch.in`, >48h old) — no action needed, unchanged.

## EAS Production Build Status — CONFIRMED
Checked `eas build:list` — production builds have run successfully on the new Expo 57 SDK, resolving the "unconfirmed since 7/31" item carried in yesterday's report:
- **iOS** — build #14, `finished`, SDK 57.0.0, commit `f13ca1c`, finished 8/3 6:00 AM. [Logs](https://expo.dev/accounts/kumravi253-source/projects/NearMatch/builds/c0ec9d68-2f0d-49de-954c-f3e5bb49df70)
- **Android** — version code 6, `finished`, SDK 57.0.0, commit `4b48a62`, finished 8/3 4:35 AM. [Logs](https://expo.dev/accounts/kumravi253-source/projects/NearMatch/builds/c8cd0a8f-97bd-4d4e-b568-78e2b8e1b4bb)
- Note: both builds predate today's `uuid` override commits (`6df27ac`/`d09643c`/`6418f24`). The override is a build-time-only dependency (native project generation via `xcode`), not a runtime dependency, so it doesn't need to ship in the app binary — but a fresh production build hasn't been triggered since those commits landed.

## Today's Focus
1. ~~Confirm actual status of Apple Developer account registration~~ → confirmed by founder via Xcode Settings → Accounts: membership is **active and valid**.
2. Confirm actual status of Razorpay KYC — still the least-visible-in-code item and the likeliest remaining true launch blocker.
3. ~~Verify whether an EAS production build has actually been run since the 7/31 install-blocker fix~~ → confirmed, see above: iOS build #14 and Android version code 6 both finished successfully on SDK 57 this morning.
4. Founder sanity-check on the IP-based (non-JWT) rate-limiting design for `create-razorpay-order`, carried over from yesterday.
5. ~~Commit the `package.json`/`package-lock.json` change adding the `uuid` override~~ → done, committed (`6df27ac`) and pushed (`d09643c`) to `main`.

## Grow Today
- Promote the new waitlist momentum counter (shipped overnight in `f13ca1c`) across social channels with a "before Sept 1" countdown framing to build urgency.
- Post an Instagram Reel showing real app UI, targeted at the Jaipur/Indore/tier-2 college-town audience — waitlist grew +22 in the last 24h (fastest growth yet) and this segment remains untapped by incumbent apps.
- Consider the "refer 2 friends, jump the queue" waitlist incentive flagged yesterday — waitlist notification infra (Resend) is live and can carry this automatically.

## Still Waiting On
- Razorpay KYC confirmation
- Google Play setup (no activity, unconfirmed)

## Notification
Sent via ntfy — id `XY6B4ZOZAOeW`, delivered successfully on first attempt.
