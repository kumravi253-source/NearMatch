# NearMatch Morning Brief — 2026-07-28

## Stats (last 24h, UTC)
| Metric | New | Total |
|--------|-----|-------|
| Users | 0 | 10 |
| Waitlist | 3 | 25 |
| Swipes | 0 | — |
| Matches | 0 | — |
| Messages | 0 | — |

## Site & Infra
- Site: Up ✅ (HTTP 200)
- SSL: valid until Oct 16 2026

## Git Activity (last 24h)
- No commits since the last report. Latest commit remains `d02543a` (test: verify commit identity fixes Vercel check).
- Working tree still carries the same uncommitted work as prior days: modified `.gitignore`, `App.js`, `src/screens/profile/ProfileSetupScreen.js`, `supabase/migrations/20260724000000_proximity_matching.sql`; untracked `.claude/launch.json`, `.mcp.json`, `.vscode/`, `security-reports/`, `supabase/functions/waitlist-notification/`, `supabase/migrations/20260725000000_launch_promo.sql`, `supabase/migrations/20260725000001_security_fix_rpc_grants.sql`.

## Security — carried forward from 2026-07-27 evening report
**Overall status: CRITICAL (unchanged)**

- 🚨 **CRITICAL, still unfixed**: `supabase/functions/create-razorpay-order/index.ts` performs no JWT/auth check (only reads the Authorization header to pass through Basic auth to Razorpay), and `Access-Control-Allow-Origin` is still `"*"`. Anyone holding the public anon key can call this endpoint and create real, uncaptured live Razorpay orders with no rate limiting. Needs founder review before launch — do not blind-fix payment code.
- ⚠️ **WARN**: 27 npm vulnerabilities (19 high, 8 moderate) still require a breaking Expo 54→57 / RN 0.81→0.86 upgrade; no non-breaking fix available.
- ⚠️ **WARN**: `supabase/migrations/20260725000001_security_fix_rpc_grants.sql` is still untracked in git and missing from `schema_migrations` — the live DB lockdown (on `get_wallet_balance_paise`, `is_premium`, `get_user_id_by_email`) is at risk of being lost on a DB rebuild since it isn't versioned.

## Launch Readiness Gaps (unchanged)
- Apple Developer account registration — pending
- Razorpay KYC verification — pending
- EAS Build for production — not yet run
- Resend domain verification for nearmatch.in email — pending
- Google Play setup — pending
- Plus the CRITICAL Razorpay auth gap and uncommitted RPC migration above

## Today's Focus
1. Add JWT auth check to `create-razorpay-order` (mirror the `delete-account`/`verify-age` pattern) and restrict CORS from `*`.
2. Commit `20260725000001_security_fix_rpc_grants.sql` and reconcile with `schema_migrations` (e.g. `supabase migration repair`).
3. Commit the in-progress proximity matching and launch promo work sitting in the working tree.

## Growth Ideas for Today
- Post an Instagram Reel using the real app UI (not mockups) with a referral code/incentive aimed at the 25 idle waitlist signups — zero net user growth for multiple days running even as waitlist grows.
- Seed 2-3 college WhatsApp groups in Jaipur or Indore with the waitlist link — low-cost, high-intent tier-2 audience, still untapped.

## Still Waiting On
- Apple Developer account registration

## Notification
Sent successfully via ntfy at 2026-07-28 morning (message id `aKGkN6EZ59vB`).
