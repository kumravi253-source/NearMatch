# NearMatch Morning Brief — 2026-07-27

## Stats (last 24h)
| Metric | New | Total |
|--------|-----|-------|
| Users | 0 | 10 |
| Waitlist | 0 | 22 |
| Swipes | 0 | — |
| Matches | 0 | — |
| Messages | 0 | — |

No user, waitlist, swipe, match, or message activity in the last 24 hours.

## Site & SSL
- nearmatch.in: HTTP 200 (Up ✅)
- SSL cert expires: Oct 16 02:16:47 2026 GMT

## Git Activity
No commits in the last 24 hours (`git log --since "24 hours ago"` empty). Latest commit remains `d02543a` (test: verify commit identity fixes Vercel check).

Working tree still has the same uncommitted work as yesterday evening:
- Modified: `.gitignore`, `App.js`, `src/screens/profile/ProfileSetupScreen.js`, `supabase/migrations/20260724000000_proximity_matching.sql`
- Untracked: `.claude/launch.json`, `.mcp.json`, `.vscode/`, `security-reports/`, `supabase/functions/waitlist-notification/`, `supabase/migrations/20260725000000_launch_promo.sql`, `supabase/migrations/20260725000001_security_fix_rpc_grants.sql`

## Security — carried forward from 2026-07-26 evening report, verified still open
**Overall status: CRITICAL (unchanged)**

- 🚨 **CRITICAL, still unfixed**: Read `supabase/functions/create-razorpay-order/index.ts` directly this run to confirm — the function performs no JWT/auth check at all (no `Authorization` verification, only reads the header to pass through Basic auth to Razorpay), and `Access-Control-Allow-Origin` is still `"*"`. Anyone holding the public anon key (trivially extractable from the app bundle) can call this endpoint and create real, uncaptured live Razorpay orders with no rate limiting. Needs founder review before launch — do not blind-fix payment code.
- ⚠️ **WARN**: 27 npm vulnerabilities (19 high, 8 moderate) still require a breaking Expo 54→57 / RN 0.81→0.86 upgrade; no non-breaking fix available, not applied per AGENTS.md guidance against blind upgrades.
- ⚠️ **WARN**: `supabase/migrations/20260725000001_security_fix_rpc_grants.sql` is still untracked in git and still missing from `schema_migrations` — the live DB lockdown (on `get_wallet_balance_paise`, `is_premium`, `get_user_id_by_email`) is at risk of being lost on a DB rebuild since it isn't versioned.
- ✅ RLS, anon access, secrets hygiene, SSL, storage bucket policy, rate limiting, and block enforcement previously passed; no new checks run this morning beyond the Razorpay function re-read.

## Launch Readiness Gaps (unchanged)
- Apple Developer account registration — pending
- Razorpay KYC verification — pending
- EAS Build for production — not yet run
- Resend domain verification for nearmatch.in email — pending
- Google Play setup — pending
- Plus the CRITICAL Razorpay auth gap and uncommitted RPC migration above

## Today's Focus
1. Add JWT auth check to `create-razorpay-order` (mirror the `delete-account`/`verify-age` pattern) and restrict CORS from `*`.
2. Commit `20260725000001_security_fix_rpc_grants.sql` and reconcile with `schema_migrations` (e.g. `supabase migration repair`).
3. Commit the in-progress proximity matching and launch promo work sitting in the working tree.

## Grow Today
- Post an Instagram Reel using the real app UI (not mockups) with a referral code/incentive to the 22 idle waitlist signups before they go cold — zero net growth for two days running.
- Seed 2-3 college WhatsApp groups in Jaipur or Indore with the waitlist link — low-cost, high-intent tier-2 audience.

## Still Waiting On
- Apple Developer account registration (also blocked: Razorpay KYC, EAS production build, Resend domain verification, Google Play setup).

## Notification
Sent successfully via ntfy at 2026-07-27 06:00 IST (message id `2oMZdkoGjbLs`).
