# NearMatch Evening Digest — 2026-07-28

## Stats (today, UTC)
| Metric | New | Total |
|--------|-----|-------|
| Users | 0 | 10 |
| Waitlist | 0 | 25 |
| Swipes | 0 | — |
| Matches | 0 | — |
| Messages | 0 | — |

## Shipped Today
No commits since 2026-07-27 00:00 local (latest commit remains `d02543a`).

Working tree still carries the same uncommitted work as prior days:
- Modified: `.gitignore`, `App.js`, `src/screens/profile/ProfileSetupScreen.js`, `supabase/migrations/20260724000000_proximity_matching.sql`
- Untracked: `.claude/launch.json`, `.mcp.json`, `.vscode/`, `security-reports/`, `supabase/functions/waitlist-notification/`, `supabase/migrations/20260725000000_launch_promo.sql`, `supabase/migrations/20260725000001_security_fix_rpc_grants.sql`

## Security — from today's 08:10 IST report (OVERALL STATUS: CRITICAL, unchanged)
- 🚨 **CRITICAL, still unfixed (3rd consecutive report)**: `supabase/functions/create-razorpay-order/index.ts` has no JWT/user-session verification. Live smoke test with only the anon key created a real Razorpay order (`order_TIlET07TRFAh4y`, ₹299, uncaptured). `Access-Control-Allow-Origin` is still `"*"`. Abuse/quota-exhaustion risk against the Razorpay account; not direct fund theft (order creation ≠ captured payment), but unauthenticated payment-adjacent code should not ship this way.
- ⚠️ **WARN**: 27 npm vulnerabilities (19 high, 8 moderate) — only fixable via `npm audit fix --force`, which forces Expo 54→57 / RN 0.81→0.86 (major, breaking). No safe auto-fix available; needs a scheduled upgrade + QA window.
- ⚠️ **WARN**: `supabase/migrations/20260725000001_security_fix_rpc_grants.sql` still untracked in git and absent from `schema_migrations`. The grants it applies ARE live and correct on the DB right now, but a `supabase db reset` would silently drop them since they aren't versioned.
- ⚠️ **WARN**: No rate-limit function for `swipes` (only a dupe-prevention unique index). Low severity at current scale (19 total swipes) but worth addressing before any growth push.

All other checks (RLS, anon access, secrets, SSL, auth/users, storage, block enforcement, DB integrity) passed clean. Full detail: `security-reports/2026-07-28.md`.

## Launch Readiness Gaps (unchanged)
- Apple Developer account registration — pending
- Razorpay KYC verification — pending
- EAS Build for production — not yet run
- Resend domain verification for nearmatch.in email — pending
- Google Play setup — pending
- Plus the CRITICAL Razorpay auth gap and untracked RPC migration above

## Tomorrow's Priorities
1. Add JWT auth check to `create-razorpay-order` (mirror `delete-account`/`verify-age` pattern) and restrict CORS from `*` to the production origin.
2. Commit `20260725000001_security_fix_rpc_grants.sql` and reconcile with `schema_migrations` (`supabase migration repair`).
3. Commit the in-progress proximity-matching and launch-promo work sitting in the working tree, then execute a growth action.

## Growth Ideas for Tomorrow
- Record an Instagram Reel using the real app UI (not mockups) with a referral code, targeted at the 25 idle waitlist signups — user count has been flat at 10 for multiple days running even as the waitlist grows.
- Seed 2-3 college WhatsApp groups in Jaipur or Indore with the waitlist link — low-cost, high-intent tier-2 audience, still untapped.

## Notification
Sent successfully via ntfy at 2026-07-28 evening (message id `KU03D0kO6R4W`).
