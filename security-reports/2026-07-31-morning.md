# NearMatch Morning Brief — 2026-07-31 06:00 IST

## Last 24h Stats
- Users: +0 new (total 10)
- Waitlist: +11 new (total 69)
- Swipes: 0 | Matches: 0 | Messages: 0

## Site / SSL
- https://nearmatch.in → HTTP 200 (Up ✅)
- SSL cert expires: Oct 16 2026 GMT (~78 days remaining)

## Security
Latest full audit on disk: `2026-07-30.md` (OVERALL STATUS: 🚨 CRITICAL). No newer audit has run since, and no commits in the last 24h touch RLS policies or the Razorpay edge function, so both critical findings appear **still open**:

1. **CRITICAL** — `public.profile_locations` has two `authenticated`-role SELECT policies with `qual: true` (`authenticated_select_profile_locations`, `replace_with_policy_name`), exposing every user's exact lat/long to any logged-in user. A correctly-scoped owner-only policy also exists but is overridden since RLS policies are OR'd. Physical-safety/stalking risk for a dating app. Not auto-fixed (RLS changes require founder review).
2. **CRITICAL** — `create-razorpay-order` edge function creates a live Razorpay order (confirmed: `order_TJYJcYluE0pG9D`, ₹299.00) when called with only the anon key and no user JWT — no authentication enforced before hitting the paid third-party API.
3. **WARN** — 19 high-severity npm advisories only resolve via a semver-major bump (`expo`→57.0.9, `react-native`→0.86.2); flagged as a planned upgrade, not auto-applied per `AGENTS.md` (Expo v54 docs must be checked first).
4. **Note** — an untracked, uncommitted migration `supabase/migrations/20260725000001_security_fix_rpc_grants.sql` exists locally (locks down `get_wallet_balance_paise`, `get_user_id_by_email`, `is_premium` RPCs) but has not been applied/committed. Unrelated to the two open criticals above — worth applying separately.

## Recent commits (last 24h)
- cfe3d5f Privacy policy: cover website waitlist and disclose Salesforce
- a232965 Switch launch waitlist from Supabase to Salesforce Web-to-Lead
- ece3dee security: auto-fix npm audit vulnerabilities (non-breaking)
- 6d30a94 security: auto-fix npm audit vulnerabilities (non-breaking)

## Today's Focus
1. Drop the 2 permissive RLS policies on `profile_locations` (GPS/stalking risk)
2. Add an `auth.uid()` check to `create-razorpay-order` before it creates orders
3. Waitlist is growing (+11) but 0 app activity in the same window — check waitlist→signup conversion funnel

## Grow Today
- Share a local-city teaser (referral/proximity-matching work in progress per recent migrations) on Instagram @nearmatch.in
- DM the 69 waitlist signups a launch-date update to keep them warm

## Still Waiting On
- Founder review/fix of the 2 critical RLS + Razorpay security findings (open since at least 2026-07-30, Razorpay issue possibly since 2026-07-29)

## Notification
Sent via ntfy — id `PaZpmSdorJwb`, delivered successfully on first attempt.
