# NearMatch Evening Report — 2026-07-31 22:03 IST

## Today's Full Stats
- Users: +0 new (total 10)
- Waitlist: +2 new (total 71)
- Swipes: 0 | Matches: 0 | Messages: 0

## Shipped Today (commits)
- 0f1b436 Revert launch waitlist from Salesforce back to Supabase
- f369c91 Clarify camera permission string for age verification
- 1e72c80 Fix duplicate react-native install for EAS builds
- 1b0e6ad Remove failing Node.js CI workflow
- cfe3d5f Privacy policy: cover website waitlist and disclose Salesforce
- a232965 Switch launch waitlist from Supabase to Salesforce Web-to-Lead

Note: the waitlist backend flip-flopped Supabase → Salesforce → back to Supabase within the same day (a232965 then 0f1b436). Worth confirming this is settled before more waitlist work lands on either backend.

## Security
No new full audit ran today — most recent full audit remains `2026-07-30.md` (OVERALL STATUS: 🚨 CRITICAL). Per this morning's brief (`2026-07-31-morning.md`), no commits today touched RLS policies or the Razorpay edge function, so both critical findings remain open:

1. **CRITICAL** — `public.profile_locations` has a permissive `authenticated`-role SELECT policy with `qual: true`, exposing every user's exact lat/long to any logged-in user (RLS policies are OR'd, so the correctly-scoped owner-only policy is overridden). Physical-safety/stalking risk for a dating app. Requires founder review — not auto-fixed.
2. **CRITICAL** — `create-razorpay-order` edge function creates a live Razorpay order with only the anon key and no user JWT — no authentication enforced before hitting the paid third-party API.
3. **WARN** — 19 high-severity npm advisories only resolve via a semver-major bump (`expo`→57.0.9, `react-native`→0.86.2); not auto-applied per AGENTS.md (must check Expo v54 docs first).
4. **Note** — untracked, uncommitted migration `supabase/migrations/20260725000001_security_fix_rpc_grants.sql` still sits locally (locks down `get_wallet_balance_paise`, `get_user_id_by_email`, `is_premium` RPCs) but has not been applied or committed.

Both criticals have now been open since at least 2026-07-30 (Razorpay possibly since 2026-07-29) with no remediation commits in the interim.

## Launch Readiness Gaps
- Apple Developer account / App Store submission: still pending (no evidence of change)
- Razorpay KYC: still pending
- EAS production build: today's `1e72c80` fixed the duplicate react-native install that was blocking EAS builds — the blocker is cleared, but no evidence a production build has actually been run since
- Resend domain verification for nearmatch.in email: unconfirmed, no activity today
- Google Play setup: unconfirmed, no activity today
- Both critical security findings above remain unresolved and should block a public launch (GPS exposure + unauthenticated payment order creation)

## Growth Ideas for Tomorrow
1. Post an Instagram Reel showing real app UI, targeted/captioned for the Jaipur/Indore college-town audience — tier-2 cities are largely untapped by incumbent apps and cost less per install.
2. DM the 71 waitlist signups with a referral incentive ("invite 2 friends, jump the queue") to convert a static waitlist into active top-of-funnel growth before app activity (currently 0 swipes/matches/messages) has anything to show them.

## Tomorrow's Priorities
1. Drop the permissive RLS policy on `profile_locations` (GPS/stalking risk) — highest-severity open item, should not ship to more users until fixed
2. Add an `auth.uid()` check to `create-razorpay-order` before it creates orders
3. Apply/commit the pending `20260725000001_security_fix_rpc_grants.sql` migration
4. Confirm the waitlist backend (Supabase vs Salesforce) is settled after today's back-and-forth
5. Run an actual EAS production build now that the install blocker is fixed

## Notification
Sent via ntfy — id `JSLA0LrkqW6a`, delivered successfully on first attempt.
