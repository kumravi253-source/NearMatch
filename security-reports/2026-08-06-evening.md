# NearMatch Evening Digest — 2026-08-06 22:00 IST

## Today's Stats
| Metric | New Today | Total |
|---|---|---|
| Users (profiles) | 0 | 11 |
| Waitlist | 0 | 100 |
| Swipes | 0 | — |
| Matches | 0 | — |
| Messages | 0 | — |

Flat day across the board — no signups, no swipe activity, no new waitlist entries.

## Shipped Today
No commits today (rest day). Working tree has uncommitted local changes (`App.js`, `package.json`/`package-lock.json`, `website/index.html`, `website/styles.css`) plus untracked `functional-reports/` and several backfilled `security-reports/` files from earlier days — none of this reflects new work landed today.

## Security Report Summary (2026-08-05 morning)
Overall status: **CRITICAL** (2 open findings carried forward, no new security report generated today):
- 🚨 `referrals` table has a leftover `replace_with_policy_name` RLS policy (`qual: true`) that grants every authenticated user blanket read access, overriding the correct restrictive policy. Needs founder action: drop it in Supabase Studio.
- 🚨 `profiles` RLS does not enforce block relationships — the block-safety logic lives only in the `get_candidate_profiles()` RPC, so direct REST reads (`/rest/v1/profiles?id=eq.<uuid>`) bypass it entirely. Needs a founder decision: enforce at RLS layer or accept as-is.
- ⚠️ No swipe-velocity rate limiter (message and payment rate limits exist, swipes don't).
- ⚠️ `create-razorpay-order` returns 200 for unauthenticated smoke-test calls, writing junk rows into `razorpay_order_attempts`.
Per standing rule, RLS changes are excluded from auto-fix and require founder review.

## Functional Health Summary (2026-08-05 morning)
Overall status: **CRITICAL** — a previously-fixed bug regressed in production:
- `get_candidate_profiles()` and `get_pending_likes()` are back to `cross join viewer_location v` instead of the `left join ... on true` fix from migration `20260802000002_fix_candidate_location_cross_join.sql`. The migration is still recorded as applied and the local file still has the fix — the live database function was altered outside the tracked migration flow (likely a manual SQL Editor run or dashboard rollback).
- Direct consequence: any user without a saved `profile_locations` row gets **zero candidates** back from Discover. This includes the Apple App Review demo account (`appreview@nearmatch.in`), which has no location row at all — so a reviewer opening Discover right now would see an empty deck, a near-certain rejection reason.
- App Store Connect browser session has been expired for 3 consecutive days (08-03, 08-04, 08-05), blocking confirmation of review status and TestFlight build 11 health. Last confirmed status: "Waiting for Review" as of ~2026-08-03 02:20 IST.

## Launch Readiness Assessment
- **Apple Developer account**: confirmed active as of 2026-08-03. App Store review status itself is stale/unconfirmed for 3+ days due to expired browser session — needs manual re-login.
- **Razorpay KYC**: still listed as unconfirmed/pending — no new evidence today.
- **EAS production build**: iOS build #14 and Android version code 6 both finished successfully on SDK 57 as of 2026-08-03. Not rebuilt since the `uuid` override commits (`6df27ac`/`d09643c`/`6418f24`) landed — those are build-time-only dependency pins, not required for a rebuild, but worth noting the last verified build predates them.
- **Resend domain verification**: no new activity/confirmation found today — status unchanged from prior reports.
- **Google Play setup**: no activity — still unconfirmed.
- **Most urgent open issue**: the CROSS JOIN regression is live-app-breaking right now, and it directly affects the pending Apple App Review demo account's Discover deck. This is higher priority than the RLS findings.

## Growth Ideas for Tomorrow
1. Post an Instagram Reel showing the real Discover/swipe UI in action, geo-targeted toward Jaipur, Indore, Surat, and Lucknow audiences — tier-2/3 cities are largely untapped by incumbent dating apps and respond well to authentic in-app footage over polished ads.
2. Recruit 1-2 campus ambassadors through WhatsApp college groups in those same cities, offering early access + a referral incentive for each waitlist signup they bring in — cheap, trust-based distribution in a demographic that's hard to reach via paid ads.

## Message Sent
```
🌙 NearMatch Day Wrap

📊 Today
+0 users (total: 11) | +0 waitlist (total: 100)
Swipes: 0 | Matches: 0 | Messages: 0

✅ Shipped Today
Rest day — no commits

⚠️ Top Gaps
• PROD BUG: CROSS JOIN regressed in get_candidate_profiles/get_pending_likes — empty Discover deck for users w/o location, incl. Apple's review demo acct
• 2 open RLS gaps: referrals blanket-read + profiles block-filter bypass

🚀 Tomorrow
1. Fix CROSS JOIN regression in prod (blocks App Review demo deck)
2. Re-login App Store Connect (session expired 3d, status unconfirmed since 8/3)
3. Drop referrals placeholder RLS policy; decide on profiles block rule

📈 Reach More Users
• IG Reel of real Discover/swipe UI, targeted at Jaipur/Indore/Lucknow
• Recruit campus ambassadors via WhatsApp groups for waitlist referrals

🎯 Launch Status
Apple: acct active, review stale (3d) | Razorpay: KYC pending | Build: EAS 8/3, stale
```

**Delivery status**: Sent successfully via ntfy (message id `yo4rnPXuMwwk`).

## Follow-Up (same-session, after digest sent)

Founder asked to act on the two open items above. Findings:

1. **CROSS JOIN regression — already fixed, not by this session.** Verified live `pg_proc` definitions for `get_candidate_profiles()` and `get_pending_likes()` via the Supabase Management API: both already have `left join viewer_location v on true`, not `cross join`. Traced to commit [`799657a`](https://github.com/kumravi253-source/NearMatch/commit/799657a) ("Re-apply candidate-location CROSS JOIN fix (regressed in prod)"), authored 2026-08-06 01:43 IST — hours before this evening's digest was generated. The digest's "Top Gaps" section above is stale because it was built from the saved 2026-08-05 morning report rather than a fresh live check. No production change made this session.

2. **RLS gaps — also already fixed, also not by this session.** Queried `pg_policies` live: the `referrals` table's `replace_with_policy_name` blanket-read policy is gone (only the correct `referrer_id = auth.uid()` policy remains), and the `profiles` SELECT policy already carries the block-aware `qual` (`id = auth.uid() OR NOT EXISTS (... blocks ...)`) instead of `qual: true`. Neither fix existed as a migration file, so a fresh `supabase db reset` would have silently regenerated both holes. Added and pushed migration [`20260806000000_track_referrals_and_profiles_rls_fixes.sql`](../supabase/migrations/20260806000000_track_referrals_and_profiles_rls_fixes.sql) (commit `45f29ac`) to close that drift risk — idempotent, matches live state exactly, no production behavior change.

3. **App Store Connect status — manually confirmed by founder.** Two automated attempts this session (Chrome extension: navigation to `appstoreconnect.apple.com` blocked outright; sandboxed browser: redirected to `/login?authResult=FAILED`) both failed to access the account, consistent with the session-expired pattern seen 2026-08-03 through 2026-08-05. Founder then checked manually and confirmed: **status is still "Waiting for Review"**, unchanged since submission (~2026-08-03 02:20 IST, now ~3.5 days). Note: `functional-reports/2026-08-06.md` (written earlier the same day, 02:02 IST) claims the session was restored and review status reconfirmed at that time — but this session's later attempts found the session expired again, so access is intermittent rather than durably fixed.

**Net effect on "Top Gaps" / "Launch Status" above:** both engineering gaps are closed (tracked in git, not just live). Only genuinely open item from this digest is the App Store review itself still sitting in "Waiting for Review," which isn't something engineering can accelerate.

4. **Razorpay create-order rate limiting — re-checked, confirmed working, one low-priority note left open.** The 08-05 report's WARN turned out to be two separate non-issues plus one real minor gap: (a) the "unauthenticated 200" is intentional — `create-razorpay-order` is the public nearmatch.in checkout with no NearMatch login, so there's no `auth.uid()` to require, and CORS is already locked to `https://nearmatch.in`; (b) the rate limit itself (`check_razorpay_order_rate_limit`, migration `20260802000000_razorpay_order_rate_limit.sql`) is live and was already verified working per its own commit message (5 attempts/IP per 10-minute window, 6th gets 429). Founder decision: leave as-is. **Noted, not fixed:** the table's self-pruning only clears old rows for the IP that's *currently* calling, so one-off callers (the daily healthcheck, genuine one-time checkout attempts) that never call again leave a permanent orphan row — 8 rows accumulated over 5 days, no PII beyond IP+timestamp. Not urgent; would need a periodic `delete where created_at < now() - interval '1 day'` cron to close fully.

## Next check scheduled: tomorrow 10 PM IST
