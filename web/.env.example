# Same Supabase project as the mobile app — one backend, three clients.
# The anon key is publishable by design; every table is protected by RLS.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
